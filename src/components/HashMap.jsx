import React from 'react'

function HashMap({ data, areas }) {
//   console.log(areas)


//   function findCommonValue(obj1, obj2) {
//     // Проверяем наличие свойства 'area' и 'id' в нужном формате
//     if (obj1.area && obj1.area.id && obj2.id) {
//       if (obj1.area.id === obj2.id) {
//         return obj1.area.id // или вернуть любой из объектов, или оба
//       }
//     }
//     return null // Ничего не найдено
//   }

//   const commonValue = findCommonValue(b, a)
//   console.log(commonValue) // Вывод: "526237d3e0e34c524382c12c"

  return <div>HashMap</div>
}

export default HashMap
